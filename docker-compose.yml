services:
  app:
    build: .
    ports:
      - "5000:3000"
    volumes:
      - .:/app
    depends_on:
      - redis

  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data

volumes:
  redis-data:
